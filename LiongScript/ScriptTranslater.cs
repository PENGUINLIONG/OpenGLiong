using System;
using System.IO;
using System.Collections.Generic;

namespace LiongStudio.Script
{
    public class ScriptTranslater
    {

        
        /*
        public void TranslateScript()
        {
            while (!sr.EndOfStream)
            {
                string[] items = sr.ReadLine().Split(' ', '\t');
                if (items[0].Length == 1)
                {
                    items[0].Trim();
                    switch (items[0])
                    {
                        case "#":
                            if (nestStack.Count <= 1)
                                nestStack.Pop();
                            break;
                        case ">":
                            if (nestStack.Count != 1)
                                break;
                            while (nestStack.Peek() == NestLayerType.IfElse)
                                nestStack.Pop();
                            break;
                        case "~":

                            break;
                    }
                }
            }
        }*/
    }
}